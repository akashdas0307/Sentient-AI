# STATE_INVENTORY.md — Phase Infra-1, D1

**Committed as:** `feat(infra-1-d1): state inventory and compatibility audit`
**Lead:** Explore · **Review:** Hephaestus
**Date:** 2026-04-21

---

## 1. `.omc/` State Inventory

### 1.1 `.omc/plans/`

| Field | Value |
|-------|-------|
| **path** | `.omc/plans/` |
| **purpose** | Ralplan outputs — per-phase execution plans generated by `/oh-my-claudecode:ralplan --deliberate` |
| **format** | Markdown with OMC frontmatter (YAML-like headers) |
| **written-by** | `/oh-my-claudecode:ralplan --deliberate` (via OMC planner + architect consensus) |
| **read-by** | `/team ralph` agents, `/oh-my-claudecode:team` executors |
| **omoa-equiv** | `.agent-state/shared/plans/` (Prometheus plan output with unified frontmatter) |
| **action** | MIRROR to `.agent-state/shared/plans/` (via state-sync). Keep originals for OMC transition. |
| **do-not-touch** | YES — OMC reads this during transition |

**Files (9):**
- `ralplan-phase-4b.md` through `ralplan-phase-9.md` (7 phase plans)
- `phase-11-migration-plan.md` (Phase 11 specific)
- `open-questions.md` (cross-phase tracking)

### 1.2 `.omc/sessions/`

| Field | Value |
|-------|-------|
| **path** | `.omc/sessions/` |
| **purpose** | Session JSON files — one per Claude Code session ran by OMC |
| **format** | JSON (UUID-named files) |
| **written-by** | OMC hooks (SessionStart, SessionEnd) |
| **read-by** | `omc session search`, session recovery |
| **omoa-equiv** | OpenCode has its own session system (SQLite-backed via Kimaki) |
| **action** | ARCHIVE only. No migration needed — OpenCode sessions are separate. |
| **do-not-touch** | YES — archived, not migrated |

**Files:** 93 session JSON files (UUIDs from Phase 4b through Phase 11)

### 1.3 `.omc/state/`

| Field | Value |
|-------|-------|
| **path** | `.omc/state/` |
| **purpose** | Runtime state — checkpoints, HUD cache, mission state, subagent tracking, agent replays |
| **format** | JSON (checkpoints), JSONL (agent replays), JSON (state files) |
| **written-by** | OMC hooks (checkpoint, HUD, mission tracking) |
| **read-by** | OMC dashboard, session recovery, subagent orchestration |
| **omoa-equiv** | `.agent-state/omc/state/` (OMC_STATE_DIR points here) + `.agent-state/omoa/` (OMOA-specific) |
| **action** | COPY to `.agent-state/omc/state/`. Set `OMC_STATE_DIR` to `.agent-state/omc/`. |
| **do-not-touch** | Partial — state files are copied, originals preserved |

**Key subdirectories and files:**

| File/Dir | Count | Purpose |
|----------|-------|---------|
| `checkpoints/` | 69 files | Timestamped session checkpoints (ISO 8601 filenames) |
| `sessions/` | 7 subdirs | Per-session state subdirectories with turn data |
| `agent-replay-*.jsonl` | 1 file | Agent interaction replay log |
| `hud-stdin-cache.json` | 1 file | HUD terminal input cache |
| `last-tool-error.json` | 1 file | Last tool error state |
| `mission-state.json` | 1 file | Current mission tracking |
| `subagent-tracking.json` | 1 file | Active subagent state |

### 1.4 `.omc/artifacts/`

| Field | Value |
|-------|-------|
| **path** | `.omc/artifacts/` |
| **purpose** | `omc ask` output artifacts — codex/gemini review outputs |
| **format** | Markdown (timestamped filenames) |
| **written-by** | `omc ask codex`, `omc ask gemini` |
| **read-by** | Reviewing agents, Sisyphus for synthesis |
| **omoa-equiv** | `.agent-state/omc/artifacts/` + `@codex-worker` / `@gemini-worker` output goes to `.agent-state/shared/results/` |
| **action** | COPY to `.agent-state/omc/artifacts/`. Future artifacts go to shared/results/. |
| **do-not-touch** | YES — archive originals |

**Files (4):**
- `codex-review-the-d5-timestamp-normalization-fix-*.md` (1)
- `codex-review-this-phase-10-d1-diagnosis-*.md` (1)
- `gemini-review-the-d5-timestamp-normalization-fix-*.md` (1)
- `gemini-review-this-phase-10-d1-diagnosis-*.md` (1)

### 1.5 `.omc/notepad.md`

| Field | Value |
|-------|-------|
| **path** | `.omc/notepad.md` |
| **purpose** | Priority + working memory notepad — loaded at every session start |
| **format** | Markdown (structured: Priority Context, Working Memory, Manual sections) |
| **written-by** | `notepad_write_priority`, `notepad_write_working`, `notepad_write_manual` |
| **read-by** | All OMC agents at session start |
| **omoa-equiv** | `.agent-state/shared/notepad/priority.md` + `.agent-state/shared/notepad/working.md` |
| **action** | MIRROR to `.agent-state/shared/notepad/` (via state-sync on every invocation) |
| **do-not-touch** | Partial — state-sync copies, original preserved |

### 1.6 `.omc/project-memory.json`

| Field | Value |
|-------|-------|
| **path** | `.omc/project-memory.json` |
| **purpose** | Auto-detected tech stack, conventions, build info, notes, directives |
| **format** | JSON |
| **written-by** | `deepinit` / OMC auto-scan |
| **read-by** | Agents for project context |
| **omoa-equiv** | `.agent-state/shared/memory.md` (symlinked to `MEMORY.md` at project root) |
| **action** | Port key sections to `MEMORY.md` format. Keep JSON as archive. |
| **do-not-touch** | YES — archive |

### 1.7 `.omc/prd.json`

| Field | Value |
|-------|-------|
| **path** | `.omc/prd.json` |
| **purpose** | Cached PRD data for quick agent access |
| **format** | JSON |
| **written-by** | `deepinit` |
| **read-by** | Planner agents |
| **omoa-equiv** | Not directly needed — OMOA agents read `doc/PRD.md` directly |
| **action** | ARCHIVE only |
| **do-not-touch** | YES — archive |

---

## 2. `.claude/` State Inventory

### 2.1 `.claude/CLAUDE.md`

| Field | Value |
|-------|-------|
| **path** | `.claude/CLAUDE.md` |
| **purpose** | Claude Code session instructions (65 lines) — model routing, approval gates |
| **format** | Markdown |
| **written-by** | Human (Akash) |
| **read-by** | Claude Code on every session start; OpenCode compat layer also reads this |
| **omoa-equiv** | `AGENTS.md` at project root (OpenCode-native rules). OpenCode auto-reads `.claude/CLAUDE.md` as fallback. |
| **action** | PRESERVE. `AGENTS.md` becomes primary, `.claude/CLAUDE.md` auto-read via compat layer. |
| **do-not-touch** | YES — compat layer reads this |

### 2.2 `.claude/settings.local.json`

| Field | Value |
|-------|-------|
| **path** | `.claude/settings.local.json` |
| **purpose** | Claude Code local permissions (Bash, Playwright MCP, etc.) |
| **format** | JSON |
| **written-by** | Claude Code settings UI |
| **read-by** | Claude Code permission engine |
| **omoa-equiv** | `.opencode/opencode.json` `permission` section |
| **action** | Port relevant permissions to OpenCode config. Keep original for OMC transition. |
| **do-not-touch** | YES — OMC still uses this |

### 2.3 `.claude/skills/omc-reference/SKILL.md`

| Field | Value |
|-------|-------|
| **path** | `.claude/skills/omc-reference/SKILL.md` |
| **purpose** | OMC reference skill — agent catalog, tools, commit protocol |
| **format** | Markdown (SKILL.md) |
| **written-by** | OMC deepinit / manual |
| **read-by** | OMC skill loader |
| **omoa-equiv** | `.opencode/skills/omc-reference/SKILL.md` (OpenCode reads `.claude/skills/` via compat layer) |
| **action** | PRESERVE. OpenCode compat layer auto-discovers `.claude/skills/`. |
| **do-not-touch** | YES — compat layer reads this |

### 2.4 `.claude/worktrees/`

| Field | Value |
|-------|-------|
| **path** | `.claude/worktrees/` |
| **purpose** | Claude Code worktree mappings |
| **format** | Directory (currently empty) |
| **written-by** | Claude Code worktree management |
| **read-by** | Claude Code |
| **omoa-equiv** | OpenCode/Kimaki manages worktrees independently (SQLite database) |
| **action** | No migration needed |
| **do-not-touch** | YES — empty, no impact |

---

## 3. Other Root-Level Assets

| Asset | Exists? | Action |
|-------|---------|--------|
| `.mcp.json` | ❌ NOT FOUND | Not needed — MCP servers configured in `opencode.json` |
| `MEMORY.md` | ❌ NOT FOUND | CREATE — Kimaki reads this on every session start |
| `.env` | ❌ NOT FOUND | Do not create (RED gate) |
| `.env.example` | ✅ Found | PRESERVE — append `OMC_STATE_DIR` after migration |
| `CLAUDE.md` | ✅ Found (project root) | PRESERVE — OpenCode compat layer reads this |
| `scripts/` | ✅ Found (9 scripts) | PRESERVE — add `migrate-state.sh` and `kimaki-notify.sh` |

---

## 4. Inference Gateway Model Label Verification

**Ollama `/api/tags` output (2026-04-21):**
```
glm-5.1:cloud ✅
kimi-k2.5:cloud ✅
minimax-m2.7:cloud ✅
gemini-3-flash-preview:cloud ✅ (not used in this phase's routing)
minimax-m2.5:cloud ✅ (secondary)
glm-5:cloud ✅ (superseded by GLM-5.1)
gemma4:31b-cloud ✅ (ELIMINATED — last in all benchmarks)
```

**config/inference_gateway.yaml labels:**
- `cognitive-core` → `glm-5.1:cloud` ✅ matches
- `world-model` → `minimax-m2.7:cloud` ✅ matches
- `thalamus-classifier` → `kimi-k2.5:cloud` ✅ matches
- `checkpost` → `kimi-k2.5:cloud` ✅ matches
- `queue-zone` → `kimi-k2.5:cloud` ✅ matches
- `tlp` → `glm-5.1:cloud` ✅ matches
- `consolidation` → `kimi-k2.5:cloud` ✅ matches
- `consolidation-semantic` → `kimi-k2.5:cloud` ✅ matches
- `consolidation-procedural` → `glm-5.1:cloud` ✅ matches

**Result: NO MISMATCH.** All 9 model labels in config match available Ollama tags.

---

## 5. Do-Not-Touch List

The following must NOT be modified during migration — they remain active OMC assets:

1. `.omc/plans/*.md` — OMC ralplan still reads these
2. `.omc/sessions/*.json` — OMC session recovery
3. `.omc/state/` (all) — OMC runtime state
4. `.omc/artifacts/` (all) — OMC ask outputs
5. `.claude/CLAUDE.md` — Claude Code reads this
6. `.claude/settings.local.json` — Claude Code permissions
7. `.claude/skills/` — OpenCode compat layer reads these
8. `CLAUDE.md` (project root) — Both Claude Code and OpenCode read this
9. `config/inference_gateway.yaml` — System runtime config (YELLOW gate to modify)

---

## 6. Migration Action Table

| Source | Target | Action | Method |
|--------|--------|--------|--------|
| `.omc/plans/` | `.agent-state/omc/plans/` + `.agent-state/shared/plans/` | COPY + MIRROR | `migrate-state.sh` |
| `.omc/notepad.md` | `.agent-state/shared/notepad/` | MIRROR (sync) | `state-sync` agent (continuous) |
| `.omc/project-memory.json` | `MEMORY.md` (project root + `.agent-state/shared/memory.md`) | PORT + SYMLINK | Manual port → `migrate-state.sh` creates symlink |
| `.omc/state/` | `.agent-state/omc/state/` | COPY + set `OMC_STATE_DIR` | `migrate-state.sh` |
| `.omc/sessions/` | `.agent-state/omc/sessions/` | ARCHIVE (copy only) | `migrate-state.sh` |
| `.omc/artifacts/` | `.agent-state/omc/artifacts/` | COPY | `migrate-state.sh` |
| `.omc/prd.json` | (archive) | ARCHIVE | No action |
| `.claude/CLAUDE.md` | (preserve as-is) | PRESERVE | No action — compat layer reads |
| `.claude/settings.local.json` | `.opencode/opencode.json` | PORT permissions | Manual |
| `.claude/skills/` | (preserve as-is) | PRESERVE | No action — compat layer reads |
| (none) | `MEMORY.md` | CREATE | Write project context for Kimaki |
| (none) | `.agent-state/omc/` + `.agent-state/omoa/` + `.agent-state/shared/` | CREATE tree | `migrate-state.sh` |

---

*State inventory complete. Ready for D2 (Unified State Tree Design).*